#include <iostream>
#include <algorithm>

using namespace std;

int n, k;
pair<int, int> arr[102];

int main(){
    cin >> n >> k;

    for(int i=0; i<n; i++){
        cin >> arr[i].first >> arr[i].second;
    }

    

    return 0;
}